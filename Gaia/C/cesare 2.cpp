#include <stdio.h>
#include <string.h>

void main (){

  string parola;
  int len = 0;

  std::cout << "Inserire la parola da codificare: \n";
  std::cin >> parola;

  len = strlen(parola);

  std::cout << "La lunghezza della parola e': " << len '\n';

  char cifrato [len];
  int c 

  for (int i = 0; i < len; i++){


  }
}
