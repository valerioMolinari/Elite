#include <stdio.h>
#include <stdlib.h>

int main(void) {
	system("clear");


do {
		if (!alfabeto_inglese)
			puts ("l'alfabeto contiene lettere mancanti");
			printf("inserisci l'alfabeto:\n" );
			scanf("%s", alfabeto_mescolato );

			for (size_t i = 0; i < alfabeto_inglese; i++) {
				/*verifica[i] != alfabeto_inglese[i]*/
				contatore = 0;
				while (contatore == 0) {

			if (isalpha(alfabeto_mescolato[MESCOLATO])) {
				printf("%s è un alfabeto valido\n", alfabeto_mescolato );
				contatore = 1;
			}
				else
					printf("%s non è un alfabeto valido\n ", alfabeto_mescolato );
				}
					for (size_t i = 1; i < alfabeto_inglese; i++)
					for (size_t j = 0; j < 25; ++j)

					for (size_t i = 0; i < alfabeto_inglese; i++)
						if (verifica[i] != alfabeto_inglese[i]) {
							rightalfabeto_inglese = 0;
							break;
						}else
							rightalfabeto_inglese = 1;
				}
} while(!rightalfabeto_inglese );

	for (size_t i = 0; i < l; i++)
		if (parola_da_criptare[i] >= 97 && parola_da_criptare[i] <= 122)
		parola_da_criptare[i] = toupper(alfabeto_mescolato[parola_da_criptare[i] - 97])

printf("La stringa inserita è stata criptata tramite il cifrario di sostituzione:\n\n> %s\n", parola_da_criptare);

}
