#include <stdio.h>
#include <stdlib.h>

int multiple(int a, int b);

int main(void) {
	system("clear");
int primo, secondo;
int result;

printf("inserisci una coppia di numeri interi\n" );
scanf("%d%d\n", &primo, &secondo );

printf("il secondo è un multiplo del primo\n" );
scanf("%d\n", result );

}
int multiple(int a, int b);

if (b / a) {
	result = b / a;
}
	else
