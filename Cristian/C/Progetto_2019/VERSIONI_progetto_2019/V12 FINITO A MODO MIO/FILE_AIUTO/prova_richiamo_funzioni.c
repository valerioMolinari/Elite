#include <stdio.h>
#include <stdlib.h>

controllo_input(char str[], int min, int max)
  {
    while (scanf("%hd",&str) != 1 || numero_caverne < min || numero_caverne >= max)
      {
        printf("SCELTA NON CORRETTA! REINSERISCI IL VALORE: ");
        while (getchar() != '\n');
      }
  }

int main ()
  {
    int 
    controllo_input ()
  }
