/* Chiedere all'utente di scegliere se utilizzare il cifrario di Cesare oppure
utilizzare il cifrario con l'alfabeto rimescolato;
se l'utente sceglie il cifrario di Cesare allora:
fissare una variabile k;
chiedere all'utente di inserire di quante posizioni k traslare l'alfabeto classico;
in base al valore di k traslare l'alfabeto;
chiedere all'utente di inserire una stringa di massimo 256 caratteri;
leggere la stringa inserita da tastiera;
*/

#include <stdio.h>
#include <stdlib.h>

int main(void) {
system("clear");

}
